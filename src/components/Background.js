import React from "react"
import { BackgroundWrapper } from '../elements'

export const Background = () => {

  return (
      <BackgroundWrapper>
        <canvas id="hero-lightpass">
        </canvas>
      </BackgroundWrapper>
    )
}
