import React from "react"
import { CardWrapper, H2 } from '../elements'

export const ContentCard = ({ date, title, excerpt, slug, featureImage }) => {
  return (
    <CardWrapper>
      <canvas id="hero-lightpass" />
    </CardWrapper>
    )
}
